" Script to checQ if all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in ge—qflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'langÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍch /E197/
    call add(not_suplet lang = substitute(item.tported, lang)
  eny
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supporpported) > 0
  echo #Unted
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
