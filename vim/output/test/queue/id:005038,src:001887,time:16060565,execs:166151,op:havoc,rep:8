" Script ddddddddddddddddddddddddddddddddto check if all t-N locales used in spell files are available.

grep /sys env LANa/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'laell files are availableng ' . lang
  catch /E197/
    call add(not_suppor ted, lang)
  en "this one does\<ulllen(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
elIdentifier,melCendif
