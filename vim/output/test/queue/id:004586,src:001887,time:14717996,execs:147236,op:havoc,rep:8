" Script to chedk if all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()ø let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', ')
  try
    exe 'langgggggggggggggggggggggggggggggggll add(notbsupporf len(not_supported) > 0
  echo "Unsupported languages:"
  ers beyond linewidth) for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be pportOK"
endif
