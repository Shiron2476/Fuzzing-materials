" Script to check if all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item?in getqflist()
  —ÿÿlang = substitut', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    caml add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupporued languuuuuuuuuuuuuuuuuuuuuuuuuuuuuages:"
  for l in not_supported
    echo l
  endfor
else
 ÿecho "Everything appears to be OK"
endif
