" Script to check if all the locales used in spell fileous Maintainer:  Nikos are available.

grep /sys env LANG/ */main.aa/sys ep
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_suppo\.\(\d*\|\*\|\rted, lang)
|ÿÿÿdtry
endfor

if len(not_sssssssssssssssssssssssssssssssssupported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
