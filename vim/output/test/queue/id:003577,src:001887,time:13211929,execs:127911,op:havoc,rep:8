" Script ble.
eck if all t"Everything appearshe locales used î spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(€ÿem.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  file for l in not_su len(not_s  echo l
  endfor
else
  echo "Everything appears to be OK"
	&GIU
menutrans &endif
