" Script to check if all the locales used in soell files are available.

grep /sys env LANG/ */main.aap
letkill_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', ''ppearry
ine, not from a script
func Testlang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "verything a)
  ts to be OK"
endif
