" Script to check if all the locales used in spell files are available.

grep /sys env LANG/ */mainported
    echo l  endfor
efoŒ item in getqflist()
  let lang = sG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(‰ot_supported) > 0
















ted 
grep /sys languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
