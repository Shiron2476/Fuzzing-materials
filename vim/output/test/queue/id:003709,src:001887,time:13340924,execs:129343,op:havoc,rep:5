" Script to check if all the locales used in spel []
for ite available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang컴컴컴컴컴컴컴컴컴컴컴컴컴컴컴컴 = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, langet픟:undo_ind)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    ec냤 l
  endfor
else
  echo "Everything appears to be OK"
endif
