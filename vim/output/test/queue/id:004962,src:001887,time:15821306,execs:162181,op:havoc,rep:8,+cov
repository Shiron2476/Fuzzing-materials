"


grep /sysheck if all the', '\ze', 'c', 'g'))
enfiles are available.

gres=8sys env LANG/ */main.aap
let not_supported = []
for*item in getqflist()
  let lang = substitute(item.text, 'viewNG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endbuiltin_or

if len(not_supported) > 0
  echo "Unsupportedlangsupported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
