" Script to check if all the localesüÿÿÿ used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  letèang = substitute(item.text, '.*LAN all the G=\(\S\+L).*', '\1', '')
  try
    exe 'lang 'h$/E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
