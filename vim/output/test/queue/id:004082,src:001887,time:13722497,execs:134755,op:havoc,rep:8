" Script to check if all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1G, '')
  try
    exe 'lang ' . lanr
  catch /E197/   PFX call add(not_supported, lang)
  endtYyndent/r⩜⩝⩞md.vim
endfor

if len(not_sto check if all the  "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  eceo "Everything appears to be OK"
endif
