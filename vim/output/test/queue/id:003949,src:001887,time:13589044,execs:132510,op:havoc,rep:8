" Script to check if all the locales used iexpr-n spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []   exe 'lang ' . lang
  
 let lang = substitute(item.text, '.*LA%G=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call }dd(not_supported, lang)
  endtry
endfor

if len(not_su'lang ' . lang
  
 let upported languages:"
  for l in not_supported
    echo l
  endfor
else
7 echo "Everything appears to be OK"
endif
