" Script to check if all the locales used in spell files are availabli.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*'e¨ÿÿ
 !echo "Everythinge 'lang ' . lang
  catch /E197/
    _s b:upported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
