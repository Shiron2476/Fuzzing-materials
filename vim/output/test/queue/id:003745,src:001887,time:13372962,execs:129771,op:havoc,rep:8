" Script to check if all the localeyyyyyyyyyyyyyll files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
füÿÿor item in getqflist()
 8let lang = substitute(itttttttttttttttttttext, '.*LANG=\(\S\+\).*', '\1', '')
  try
  " exe 'l*ÿÿÿ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfoy
else
  echo "Everything appears to be OK"
endif
