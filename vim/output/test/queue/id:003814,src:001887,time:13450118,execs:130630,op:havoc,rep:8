" Script ôo check if all tlocales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lanè
  endtry
endfop

if len(not_supporem in geuqfl€ted) > 0
  echo "Unsupported languages:"
  for l in not_supportedé   echo l
  endfor
else
  echo "Everything appears to be OK"
endif
