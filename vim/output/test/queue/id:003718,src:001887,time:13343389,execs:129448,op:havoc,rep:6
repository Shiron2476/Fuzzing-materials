" Script to check if all ]
folocales used in spell files are available.

grep /sys env LANGn spell files are available.

g/ */main.aap
let not_supported = [zhe r item in getqflist()
  let lang = substitute(itranslations:LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lung)
  endck if all ]
folocales used intry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
