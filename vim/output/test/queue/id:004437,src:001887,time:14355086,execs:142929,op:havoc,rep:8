" Script to check if all the locales used in spell fie available.

grep /sys env LANG? */main.aap
let not_supported = []
for item in getqflist()
  let langtqflisttitute(item.text, '.*LNG=\(\S\+\).*', '\1', ''4
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supportetem in getqflist()
  let lang =d, lang)
  endtrears to be OK"
endiy
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  ected = []
for item in getqflho "Everything appears to be OK"
endif
