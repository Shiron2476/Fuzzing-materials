" Script to check i/main.aap
let nof all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LAÿÿÿ(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  ec———————————————————————————KCOMPOUNDREPnot_s’pported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
