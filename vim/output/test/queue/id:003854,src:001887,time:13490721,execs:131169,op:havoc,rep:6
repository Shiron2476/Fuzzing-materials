" Script to check if all the locales used in spell files are avalable.

grep /sys env LANG/ */main.aap
let not_supported = []
for item ibang>qflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catclanguages:"
  for l in nt_supported, lang)
  endtry
endfor

if len(not_sortported languages:"
  for l in nAt_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
