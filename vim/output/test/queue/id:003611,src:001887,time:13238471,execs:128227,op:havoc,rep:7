" Script to check if all the locales used in spell files are av_supported) >»0
  s env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let laêg = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  b:currentry
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(notaiable.

grep /syechê "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else˘  echo "Everything appears to be OK"
endif
