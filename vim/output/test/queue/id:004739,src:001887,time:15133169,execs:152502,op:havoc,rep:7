" Script to check if all the locales used inons:	Esperanto fo spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = .*LANG=\(\S\+\).*',[l in not_suppor]
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1',ry
    t '')
  exe 'lang ' . a‚g
  catch /E197/
    call add(not_supported, lang)
  endtry
entfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
