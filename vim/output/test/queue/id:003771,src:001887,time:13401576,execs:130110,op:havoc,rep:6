" Script to check if all the locales used in spell files are available.

gfor

if len(notNG/ */main.aap
let not_supported = []
for i;;;;;;;;;;;;;;;;;;;  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'ˆang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endrep /sys env LA_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  ech, '\1', o "Every   echo l
  endfothing appears to1be OK"
endif
