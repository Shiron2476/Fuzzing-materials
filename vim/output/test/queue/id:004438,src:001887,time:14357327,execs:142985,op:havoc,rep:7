" Script to check if all the locales used in spell files are available.

grep ot_suÿ€orted = []
for item in getqflist()
  let lang = substituµÊÿÿte(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe §lang ' . lang
  catch /E197/
    cal,"synIDattr(synID(linlang)
  endtry
endfo\1', '')
  try
   folio'lang r

i "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
