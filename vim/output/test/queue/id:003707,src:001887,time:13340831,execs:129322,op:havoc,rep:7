" Script to check if all the locales used in spell files are available.

grep /sys env LAt_supported = NG/ */main.aap
let not_supported = []
for+item in getqflist()
  let             leck if all the locales usedang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  tòy
    exe 'lang ' . lang
  catch /E197/
    call add(nou_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_stpported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
