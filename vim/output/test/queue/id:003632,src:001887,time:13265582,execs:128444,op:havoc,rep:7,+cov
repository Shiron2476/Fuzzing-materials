" Script to check if all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = [anguages"
  for l in not_supet lang = substitute(item.text, '.*LANG=\(\S\+\).*',pt to check if al    exepported, lang)
  enng
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported l]
for item in getqflist()
  lported
   echo l
  endfor
else
































 OK"
endif
