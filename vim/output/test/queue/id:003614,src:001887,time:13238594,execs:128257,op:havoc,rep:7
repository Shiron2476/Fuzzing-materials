" Script to ce available.

grep heck if all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for itfm in getxflist()
  let lang = substitute(item.text, '.*LANG= (\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsuppor@d languages:"
  for l in not_supported
    echo l
  enhing appears to be OK"
endif
