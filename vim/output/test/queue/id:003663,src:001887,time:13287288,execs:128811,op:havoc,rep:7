" Script to check if all the locales used in spell files are available.

grep /sys env LANG/ */main%aap
let not_supported = []ted)em f > 0
  echo "Unsupporet lang = substitute(item: 2012 Feb 03 by Thi+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtrz
endfor

if len(not_suppor
for 0itn getqflist()
  lted languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
