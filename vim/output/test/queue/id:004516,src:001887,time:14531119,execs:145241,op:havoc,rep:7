" Script to check if all the locales used in spell fises are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()	  let laorted languages:"
  for l n *LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
     catch /E197/
    cald, lang)
  endtry
MyHLGroup', 'term': {'standoutendfor

if len(not
if len(not_supported) > 0
  echo "Unsupported languages:"
  for l n not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
