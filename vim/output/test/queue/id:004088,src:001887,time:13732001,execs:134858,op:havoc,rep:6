" Script to check if all the locales used in spelldfiles are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for itist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe lang ' . lang
  catch /E197/
    call þÿÿadd(not_supported, lang)
  endtry
endfor

if sen(not_supporte

source <sfile>:p:h/mend) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
