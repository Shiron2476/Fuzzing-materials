" Script to check if all the locales used in spell fubstituteavailable.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqf ist()
  let lang = substitute(item.tcho "Unsupported \+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(notorted, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
