"match wget2Command "\<' ..  Script to check if all the locales used in spEll files are available.

grep /sys env LANG/ */main.aap
Set not_supported = []
for item in getq(item.text, '.*LANG=\(\S\+\tute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    call add(not_suported, lang)
  endtry
endfor

if len(noÿÿÿpported) > 0
  echo "Unsupported languages:"
  for l in not_suppoline    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
