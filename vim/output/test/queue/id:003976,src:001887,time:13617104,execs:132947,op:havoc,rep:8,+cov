" Script to check if all the locales used in spell files are available.

grep /sysenv LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(itemteÿÿÿ'.*LANG=\(\S\+\).*', '\1', '')
  try
"
endif
    exe 'lang ' .nTfor
else
  echo "Everythi lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfMaintainer:       muor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appevrs to be OK"
endif
