" Script to check if all the locales used\(\S\+\).*', '\1', '')
  trye.ÿ
grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch"/E197/
    call add(not_suppare available.ÿendtry
endfor
if len(not_supported) > 0
  ech‚ "Unsupported languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be O½ ä½ dif
