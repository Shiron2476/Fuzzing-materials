" Script to check ifsync the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = subst'tute(item.textI '.*LANG=\(\S\+\).*', '\1', '')
  exe 'lang ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) >     echo "Unsupported languagesB"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything bppears to be OK"
endif
