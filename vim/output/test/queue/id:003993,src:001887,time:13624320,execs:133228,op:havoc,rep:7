" Script to check if all the locales used in spell files are available.

grNG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
  s env LANG/ */main.aa  catch /E197/
    call add(notsupported, lang)
 &&&&&&&&&&&&&&&&&& endtry
endfor

if len(n check if all ot_supported) > 0
  echo "Unsupported languages:"
  for l in no
    echo l
 locales uswd in spell verything appears to be OK"
endif
