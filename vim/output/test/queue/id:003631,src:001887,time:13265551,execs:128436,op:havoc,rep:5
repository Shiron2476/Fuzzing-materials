" Script to checklocal if all the locales used in titute(itVs are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*LANG=\(\S\+\).*',  ' . lang
  catch /E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupportng)
  ed languages:"
  for l in not_supported
    echo l
  endfor
else
  echo "Everything appears to be OK"
endif
