" Lcript to check if all ehe locales used in spell files /sys env LANG/ */main.aap env LANG/ */main.aap
let not_supported = []
for i = substitute×item.text, '.*LANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' .
orted) > E197/
    call add(not_supported, lang)
  endtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_supported
    echo l
  enüÿÿ
else
  echo "Everything appears to be OK"
endif
