" Script to check if all the locales used in spell files are available.

grep /sys env LANG/ */main.aap
let not_supported = []
for item in getqflist()
  let lang = substitute(item.text, '.*L match cssUnitDecoratANG=\(\S\+\).*', '\1', '')
  try
    exe 'lang ' . lang
  catch /E197/
    callor

if len(n dtry
endfor

if len(not_supported) > 0
  echo "Unsupported languages:"
  for l in not_stpported
    echo l
  endfor
ÿÿ
  echo "Everything appears to be OK"
endif
